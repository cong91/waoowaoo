## 2026-03-19 — Workspace header / workflow rail production polish

- VAT workspace header UX for novel-promotion was refactored so the old overlapping top header / title bar / step bar pattern is no longer the intended structure.
- Current UX direction:
  - keep a single compact header area on the left for episode selector, title/meta, and top actions
  - move stage navigation into a dedicated right-side workflow rail / sidebar on desktop
  - keep mobile navigation in a drawer/menu pattern
- `WorkspaceWorkflowSidebar.tsx` was added as the dedicated right-side workflow rail surface.
- `WorkspaceHeaderShell.tsx` was simplified so title/meta no longer stretches full width and stays anchored in one place.
- `CapsuleNav.tsx` now supports vertical orientation for the workflow rail.
- `workspace-header-layout.ts` and `tests/unit/helpers/workspace-header-layout.test.ts` were added to formalize desktop/mobile header layout behavior.
- Multilingual copy was updated for workspace workflow controls:
  - `journeyKickoffFilm`
  - `journeyKickoffManga`
  - `workflowRail.title`
  - `workflowRail.subtitle`
  across `messages/en|vi|ko|zh/workspace.json`.
- Production deploy was rebuilt and rolled out from the current working tree after explicit user approval, with commit:
  - `2e0f328` — `fix(vat): refine workspace header layout and localize workflow rail`
- Post-deploy verification passed:
  - local runtime `http://127.0.0.1:13000/vi/workspace` returned 200
  - production `https://videoai.linkto.com.vn/vi/workspace` returned 200
  - production container `waoowaoo-app` restarted healthy on image `vat-app`
- Known follow-up still open:
  - some config-stage labels/copy remain mixed-language and may need a dedicated i18n cleanup pass
  - workflow sidebar can still receive a final premium visual polish pass if requested
