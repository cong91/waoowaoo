# VAT-133 P1.1 — Quick actions edge-case test evidence (2026-03-12)

## Scope
- Ticket: VAT-133
- Branch: `work/vat-manga-webtoon-lane-20260312`
- Focus: webtoon quick actions edge cases (`split`, `merge`, `reorder`) theo path mutation thật (delete/create order planning).

## Changes made
- Introduced deterministic mutation planner for quick actions:
  - `planWebtoonQuickActionMutation(...)`
  - Returns: `deletePanelIds`, `createPayloads`, `beforeOrder`, `expectedAfterOrder`
- `MangaPanelControls` now executes quick actions through planner (single source of truth for edge-case behavior), then applies delete/create in sequence.
- Added local debug evidence log in UI path:
  - `console.info('[VAT-133][quick-action-plan]', { ... })`

## Test commands
```bash
npx vitest run tests/unit/workspace/webtoon-panel-controls.test.ts tests/unit/workspace/stage-alias.test.ts tests/unit/workspace/stage-navigation-lane.test.ts
```

Result:
- PASS `17/17`

Focused edge-case command:
```bash
npx vitest run tests/unit/workspace/webtoon-panel-controls.test.ts -t "plans split mutation|plans merge mutation|plans reorder mutation|guards merge/reorder"
```

Result:
- PASS `4/4` (filtered)

## Edge-case verification snapshots
- split (selected=`p2`, order=`p1,p2,p3`)
  - delete: `[p2]`
  - creates: `2`
  - expectedAfterOrder: `[p1,__new_split_left_of_p2__,__new_split_right_of_p2__,p3]`

- merge (selected=`p2`, order=`p1,p2,p3`)
  - delete: `[p2,p1]`
  - creates: `1`
  - expectedAfterOrder: `[__new_merge_p1_p2__,p3]`

- reorder (selected=`p3`, order=`p1,p2,p3`)
  - delete: `[p1]`
  - creates: `1`
  - expectedAfterOrder: `[p2,p3,p1]`

## Compatibility-first note
- No API contract changes.
- No route/schema changes.
- Existing helper payload generators remain intact; planner composes existing helpers.
