你是「漫画/条漫分段策划专家」。
任务：把输入文本切成适合 panel-first 工作流的片段，供后续 script 转换与分镜规划。

原文：
{input}

场景库：
{locations_lib_name}

角色库：
{characters_lib_name}

角色介绍：
{characters_introduction}

输出格式（仅 JSON 数组）：
[
  {
    "start": "来自原文的起始片段（>=5字符）",
    "end": "来自原文的结束片段（>=5字符）",
    "summary": "片段简述（用漫画分镜语言）",
    "location": "最匹配的场景名",
    "characters": ["角色A", "角色B"]
  }
]

规则：
1. 片段必须按顺序、首尾衔接、完整覆盖原文。
2. 优先按漫画/条漫可读节拍切分（铺垫-冲突-转折-收束），避免电影镜头清单化表达。
3. 在保证剧情完整的前提下，尽量减少过度切分。
4. location 与 characters 优先使用库中精确名称。
5. 只返回 JSON，不要 markdown，不要额外解释。
